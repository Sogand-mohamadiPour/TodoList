# 📝 Vanilla JS Todo App

A simple Todo List application built with:

- HTML
- CSS
- Vanilla JavaScript

No frameworks. No libraries. Just pure DOM manipulation.

---

## 🧠 What This Project Practices

- DOM selection (`getElementById`)
- Event handling (`addEventListener`)
- Creating elements dynamically (`createElement`)
- Appending elements (`appendChild`)
- Removing elements
- Class manipulation (`classList.toggle`)
- Basic UI styling with CSS

---

Built for my class.
